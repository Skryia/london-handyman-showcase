{
  "name": "london-handyman-static",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host --port 8080",
    "build": "rm -rf dist && mkdir dist && cp -r index.html styles.css script.js assets robots.txt sitemap.xml 404.html .nojekyll dist/",
    "start": "vite preview --host --port 8080"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
